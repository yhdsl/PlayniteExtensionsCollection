name: Build Playnite Extensions

on:
  push:
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout source code
        uses: actions/checkout@v4
        with:
          path: './source'

      - name: Add msbuild to PATH
        uses: microsoft/setup-msbuild@v2

      - name: Nuget Restore
        run: |
          nuget restore .\source\source\PlayniteExtensions.sln
          nuget restore .\source\source\Metadata\VNDBNexus\VNDBNexus.sln

      - name: Build entire solution (with .NET Framework 4.6.2)
        run: |
          cd "C:\Program Files\Microsoft Visual Studio\2022\Enterprise\MSBuild\Current\Bin\"
          .\MSBuild.exe $Env:GITHUB_WORKSPACE\source\source\Metadata\VNDBNexus\VNDBNexus.sln -p:Configuration=Release -restore

      - name : Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: VNDBNexus
          path: |
            .\source\source\Metadata\VNDBNexus\bin\Release\*
